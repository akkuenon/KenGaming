# KenGaming C# terminal

This folder contains a small .NET console app that adds a lightweight terminal-style interface to the repo.

## Run it

```bash
cd code-terminal
dotnet run
```

## Example commands

```text
help
ls
pwd
echo hello from KenGaming
clear
exit
```
